# ==============================================================
# Predictive Multimodal Student Engagement and Cognitive Monitoring System
# Phase 1 -- Core dependencies (AI/CV libraries are pinned here now,
# even though their model code arrives in later phases, so the
# environment only needs to be built once).
# ==============================================================

# --- Core numerical / data ---
numpy~=1.26
pandas~=2.2
scikit-learn~=1.4
matplotlib~=3.8

# --- Computer vision / AI (used starting Phase 2+) ---
opencv-python~=4.9
mediapipe~=0.10
tensorflow~=2.16
deepface~=0.0.93
ultralytics~=8.2          # YOLOv8

# --- Backend / API ---
fastapi~=0.111
uvicorn[standard]~=0.29
pydantic~=2.7

# --- Database ---
pymongo~=4.7
motor~=3.4                # async MongoDB driver

# --- Config / environment / logging ---
PyYAML~=6.0
python-dotenv~=1.0
loguru~=0.7

# --- Testing ---
pytest~=8.2
pytest-cov~=5.0
httpx~=0.27               # FastAPI TestClient dependency
