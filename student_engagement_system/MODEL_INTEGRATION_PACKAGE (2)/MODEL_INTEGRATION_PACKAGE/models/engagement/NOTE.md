# No model here

No engagement/LSTM model exists in the source project to copy. See
`../../MODEL_INVENTORY.md` item 6 and `../../LIVE_PIPELINE.md` for the full
explanation: `ml/training/lstm_engagement/` in the source project contains
only a `.gitkeep`, and `backend/worker/tasks/lstm_task.py` is an explicit
stub that raises `NotImplementedError`.
